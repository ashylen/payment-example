import React from "react";

import styles from "./CartView.module.scss";

const CartView = () => {
  return (
    <div className={styles.wrapper}>
      <span>CartView :div</span>
    </div>
  );
};

export default CartView;
